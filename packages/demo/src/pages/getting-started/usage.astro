---
import Code from '../../components/ui/code.astro';
import Description from '../../components/ui/description.astro';
import Heading from '../../components/ui/heading.astro';
import PageWrapper from '../../components/ui/page-wrapper.astro';
---

<PageWrapper>
  <Heading>Usage</Heading>

  <section class="space-y-4">
    <Heading as="h2" displayAs="2">Setting up the theme</Heading>
    <div class="space-y-2">
      <Description
        >Add the <code>{`<ThemeProvider />`}</code> at the root of your app or wherever Equality components
        are used:</Description
      >
      <Code
        code={`import { ThemeProvider } from "@eqtylab/equality";

export function App() {
  return (
    <ThemeProvider theme="light">
      <YourApp />
    </ThemeProvider>
  );
}`}
      />
      <Description
        >This sets up all required CSS variables and makes sure the components render correctly.</Description
      >
    </div>
  </section>

  <section class="space-y-4">
    <Heading as="h2" displayAs="2">Use components</Heading>
    <div class="space-y-2">
      <Description
        >Each component includes its own scoped <code>.module.css</code> file — no need to import a global
        stylesheet.</Description
      >
      <Code
        code={`import { Button } from "@eqtylab/equality";

export function Example() {
  return (
    <>
      <Button>Primary</Button>
    </>
  );
}`}
      />
      <Description
        >Because the styles use Tailwind v4 tokens (<code>bg-primary</code>, <code
          >text-foreground</code
        >, etc.), everything is resolved automatically based on the active theme.</Description
      >
    </div>
  </section>

  <section class="space-y-4">
    <Heading as="h2" displayAs="2">Overriding styles with <code>className</code></Heading>
    <div class="space-y-2">
      <Description
        >All components accept a <code>className</code> prop for fine-grained style adjustments. This
        lets you extend or override the default styles directly from your app without breaking isolation:</Description
      >
      <Code
        code={`<Button className="bg-accent hover:bg-accent/80 text-white">
  Custom Button
</Button>
`}
      />
      <Description
        >Since each component’s styles are scoped with CSS Modules, your overrides stay local and
        won’t leak into other components.</Description
      >
    </div>
  </section>
</PageWrapper>
