---
import Code from "../../components/ui/code.astro";
import ContentWrapper from "../../components/ui/content-wrapper.astro";
import Description from "../../components/ui/description.astro";
import Heading from "../../components/ui/heading.astro";
import PageHeader from "../../components/ui/page-header.astro";
import PageWrapper from "../../components/ui/page-wrapper.astro";
import PageContentWrapper from "../../components/ui/page-content-wrapper.astro";
import SectionWrapper from "../../components/ui/section-wrapper.astro";
import TreeView from "../../components/ui/tree-view.astro";

const themeCode = `
import { ThemeProvider } from "@eqtylab/equality";

export function App() {
  return (
    <ThemeProvider theme="light">
      <YourApp />
    </ThemeProvider>
  );
}
`;

const useComponentsCode = `
import { Button } from "@eqtylab/equality";

export function Example() {
  return (
    <>
      <Button>Primary</Button>
    </>
  );
}
`;

const overridingStylesCode = `
<Button className="bg-accent hover:bg-accent/80 text-white">
  Custom Button
</Button>
`;
---

<PageWrapper>
  <PageContentWrapper>
    <PageHeader heading="Usage" />

    <SectionWrapper>
      <Heading as="h2" id="setting-up-the-theme">Setting up the theme</Heading>
      <ContentWrapper>
        <Description
          >Add the <code>{`<ThemeProvider />`}</code> at the root of your app or
          wherever Equality components are used:</Description
        >
        <Code code={themeCode} />
        <Description
          >This sets up all required CSS variables and makes sure the components
          render correctly.</Description
        >
      </ContentWrapper>
    </SectionWrapper>

    <SectionWrapper>
      <Heading as="h2" id="use-components">Use components</Heading>
      <ContentWrapper>
        <Description
          >Each component includes its own scoped <code>.module.css</code> file —
          no need to import a global stylesheet.</Description
        >
        <Code code={useComponentsCode} />
        <Description
          >Because the styles use Tailwind v4 tokens (<code>bg-primary</code>, <code
            >text-foreground</code
          >, etc.), everything is resolved automatically based on the active
          theme.</Description
        >
      </ContentWrapper>
    </SectionWrapper>

    <SectionWrapper>
      <Heading as="h2" id="overriding-styles-with-classname"
        >Overriding styles with <code>className</code></Heading
      >
      <ContentWrapper>
        <Description
          >All components accept a <code>className</code> prop for fine-grained style
          adjustments. This lets you extend or override the default styles directly
          from your app without breaking isolation:</Description
        >
        <Code code={overridingStylesCode} />
        <Description
          >Since each component’s styles are scoped with CSS Modules, your
          overrides stay local and won’t leak into other components.</Description
        >
      </ContentWrapper>
    </SectionWrapper>
  </PageContentWrapper>

  <TreeView
    items={[
      { label: "Setting up the theme", id: "setting-up-the-theme" },
      { label: "Use components", id: "use-components" },
      {
        label: "Overriding styles with className",
        id: "overriding-styles-with-classname",
      },
    ]}
  />
</PageWrapper>
