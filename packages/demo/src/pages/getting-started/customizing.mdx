---
layout: "@/layouts/mdx-layout.astro"
heading: "Customizing"
---

## Custom variables

You can override specific tokens using the `customVars` prop on `ThemeProvider`:

```tsx
<ThemeProvider
  theme="light"
  customVars={{
    "--color-primary": "hsl(280 75% 60%)",
    "--color-accent": "hsl(50 100% 70%)",
  }}
>
  <App />
</ThemeProvider>
```

Perfect for brand-specific adjustments or white-label versions.

## Extending Tailwind

Because Equality's theme uses Tailwind's native tokens (`bg-primary`, `text-foreground`, etc.), you can safely add custom Tailwind classes in your host app without conflicts:

```tsx
<div className="bg-primary text-accent hover:bg-primary/80 transition-all">
  Custom Block
</div>
```

## Best practices

- Keep overrides scoped inside your `ThemeProvider`
- Avoid redefining tokens globally in the host app
- Use HSL values for easy opacity and dynamic color blending
