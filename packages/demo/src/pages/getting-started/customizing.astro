---
import Code from '../../components/ui/code.astro';
import Description from '../../components/ui/description.astro';
import Heading from '../../components/ui/heading.astro';
import List from '../../components/ui/list.astro';
import PageHeader from '../../components/ui/page-header.astro';
import PageWrapper from '../../components/ui/page-wrapper.astro';
---

<PageWrapper>
  <PageHeader heading="Customizing" />

  <section class="space-y-4">
    <Heading as="h2" displayAs="2">Custom variables</Heading>
    <div class="space-y-2">
      <Description>
        You can override specific tokens using the <code>customVars</code> prop on <code
          >ThemeProvider</code
        >:
      </Description>
      <Code
        code={`<ThemeProvider
  theme="light"
  customVars={{
    "--color-primary": "hsl(280 75% 60%)",
    "--color-accent": "hsl(50 100% 70%)",
  }}
>
  <App />
</ThemeProvider>`}
      />
      <Description>Perfect for brand-specific adjustments or white-label versions.</Description>
    </div>
  </section>

  <section class="space-y-4">
    <Heading as="h2" displayAs="2">Extending Tailwind</Heading>
    <div class="space-y-2">
      <Description
        >Because Equality’s theme uses Tailwind’s native tokens (<code>bg-primary</code>, <code
          >text-foreground</code
        >, etc.), you can safely add custom Tailwind classes in your host app without conflicts:</Description
      >
      <Code
        code={`<div className="bg-primary text-accent hover:bg-primary/80 transition-all">
  Custom Block
</div>`}
      />
    </div>
  </section>

  <section class="space-y-4">
    <Heading as="h2" displayAs="2">Best practices</Heading>
    <List
      items={[
        'Keep overrides scoped inside your <code>ThemeProvider</code>',
        'Avoid redefining tokens globally in the host app',
        'Use HSL values for easy opacity and dynamic color blending',
      ]}
    />
  </section>
</PageWrapper>
